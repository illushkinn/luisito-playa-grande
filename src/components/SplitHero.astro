---
// Hero section with fullscreen video background
---
<section class="hero">
  <video class="hero__video" autoplay loop muted playsinline poster="/images/hero-image(1).webp">
    <source src="/images/0602-compressed.mp4" type="video/mp4" />
  </video>
</section>

<style>
  .hero {
    position: relative;
    min-height: 100vh;
    width: 100vw;
    margin-left: calc(-50vw + 50%);
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    border-bottom: 2px solid var(--color-text);
  }
  .hero__video {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: 0;
  }

  @media (max-width: 768px) {
    .hero {
      min-height: 80vh;
    }
  }
</style>
